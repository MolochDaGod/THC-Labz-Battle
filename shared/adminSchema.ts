/**
 * THC CLASH admin schema — re-exports from the canonical root shared/schema.ts
 */
export {
  adminCards,
  gameSessions,
  spellEffects,
  type AdminCard,
  type GameSession,
  type SpellEffect,
} from "../../shared/schema";
